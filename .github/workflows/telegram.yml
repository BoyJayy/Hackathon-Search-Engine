name: Telegram All-in-One Repo Feed

on:
  push:
  pull_request:
    types: [opened, reopened, synchronize, closed, ready_for_review]
  issues:
    types: [opened, reopened, closed, edited]
  issue_comment:
    types: [created]
  pull_request_review:
    types: [submitted, edited]
  pull_request_review_comment:
    types: [created]
  release:
    types: [published, prereleased]
  create:
  delete:
  fork:
  watch:
    types: [started]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  issues: read
  actions: read

jobs:
  telegram:
    runs-on: ubuntu-latest

    steps:
      - name: Build Telegram message
        id: build
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_PATH: ${{ github.event_path }}
          REPOSITORY: ${{ github.repository }}
          REPOSITORY_URL: ${{ github.server_url }}/${{ github.repository }}
          ACTOR: ${{ github.actor }}
          SHA: ${{ github.sha }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          python3 << 'PY'
          import json
          import os
          import html

          event_name = os.environ["EVENT_NAME"]
          event_path = os.environ["EVENT_PATH"]
          repository = os.environ["REPOSITORY"]
          repository_url = os.environ["REPOSITORY_URL"]
          actor = os.environ["ACTOR"]
          sha = os.environ["SHA"]
          ref_name = os.environ.get("REF_NAME", "")

          with open(event_path, "r", encoding="utf-8") as f:
              event = json.load(f)

          def esc(x):
              return html.escape(str(x or ""))

          def short_sha(s):
              return (s or "")[:7]

          def trim(s, n=300):
              s = str(s or "").strip()
              if len(s) <= n:
                  return s
              return s[:n - 1] + "…"

          def lines_join(items):
              return "\n".join(i for i in items if i)

          msg = ""
          event_type = event_name

          if event_name == "push":
              commits = event.get("commits", [])
              compare = event.get("compare", repository_url)
              branch = event.get("ref", "").replace("refs/heads/", "").replace("refs/tags/", "")
              created = event.get("created", False)
              deleted = event.get("deleted", False)
              forced = event.get("forced", False)

              title = "📤 <b>PUSH</b>"
              if created:
                  title = "🌱 <b>BRANCH/TAG CREATED BY PUSH</b>"
              if deleted:
                  title = "🗑 <b>REF DELETED BY PUSH</b>"

              commit_lines = []
              for c in commits[:10]:
                  message = trim((c.get("message") or "").split("\n")[0], 120)
                  author = c.get("author", {}).get("name", "unknown")
                  url = c.get("url", "")
                  commit_lines.append(
                      f"• <code>{esc(c.get('id', '')[:7])}</code> "
                      f"{esc(message)} — <i>{esc(author)}</i>\n"
                      f"  <a href=\"{esc(url)}\">open commit</a>"
                  )

              extra = ""
              if len(commits) > 10:
                  extra = f"\n…and {len(commits) - 10} more commit(s)"

              flags = []
              if forced:
                  flags.append("forced push")

              msg = lines_join([
                  title,
                  f"📦 <b>{esc(repository)}</b>",
                  f"🌿 <code>{esc(branch or ref_name)}</code>",
                  f"👤 <b>{esc(actor)}</b>",
                  f"🔢 commits: <b>{len(commits)}</b>",
                  f"⚠️ {' | '.join(flags)}" if flags else "",
                  "",
                  "<b>Changes:</b>",
                  "\n".join(commit_lines) if commit_lines else "No commit list in payload",
                  extra,
                  "",
                  f"🔗 <a href=\"{esc(compare)}\">compare view</a>"
              ])

          elif event_name == "pull_request":
              pr = event.get("pull_request", {})
              action = event.get("action", "")
              number = pr.get("number", event.get("number", ""))
              title = pr.get("title", "")
              url = pr.get("html_url", repository_url)
              state = pr.get("state", "")
              merged = pr.get("merged", False)
              draft = pr.get("draft", False)
              additions = pr.get("additions", 0)
              deletions = pr.get("deletions", 0)
              changed_files = pr.get("changed_files", 0)
              commits_count = pr.get("commits", 0)
              base = pr.get("base", {}).get("ref", "")
              head = pr.get("head", {}).get("ref", "")
              body = trim(pr.get("body", ""), 400)

              icon = "🔀"
              label = f"PR {action.upper()}"
              if action == "closed" and merged:
                  icon = "✅"
                  label = "PR MERGED"
              elif action == "closed":
                  icon = "❌"
                  label = "PR CLOSED"
              elif action == "ready_for_review":
                  icon = "👀"
                  label = "PR READY FOR REVIEW"

              details = []
              if state:
                  details.append(f"state: {state}")
              if draft:
                  details.append("draft")
              details_text = " | ".join(details)

              msg = lines_join([
                  f"{icon} <b>{esc(label)}</b>",
                  f"📦 <b>{esc(repository)}</b>",
                  f"#{number} <a href=\"{esc(url)}\"><b>{esc(title)}</b></a>",
                  f"👤 <b>{esc(actor)}</b>",
                  f"🌿 <code>{esc(head)}</code> → <code>{esc(base)}</code>",
                  f"📊 +{additions} / -{deletions} | files: {changed_files} | commits: {commits_count}",
                  f"ℹ️ {esc(details_text)}" if details_text else "",
                  "",
                  esc(body) if body else ""
              ])

          elif event_name == "issues":
              issue = event.get("issue", {})
              action = event.get("action", "")
              number = issue.get("number", event.get("number", ""))
              title = issue.get("title", "")
              url = issue.get("html_url", repository_url)
              state = issue.get("state", "")
              body = trim(issue.get("body", ""), 400)

              msg = lines_join([
                  f"📝 <b>ISSUE {esc(action.upper())}</b>",
                  f"📦 <b>{esc(repository)}</b>",
                  f"#{number} <a href=\"{esc(url)}\"><b>{esc(title)}</b></a>",
                  f"👤 <b>{esc(actor)}</b>",
                  f"📌 state: <b>{esc(state)}</b>",
                  "",
                  esc(body) if body else ""
              ])

          elif event_name == "issue_comment":
              issue = event.get("issue", {})
              comment = event.get("comment", {})
              is_pr = "pull_request" in issue
              kind = "PR COMMENT" if is_pr else "ISSUE COMMENT"

              msg = lines_join([
                  f"💬 <b>{kind}</b>",
                  f"📦 <b>{esc(repository)}</b>",
                  f"🔢 #{issue.get('number', '')}: <a href=\"{esc(comment.get('html_url', repository_url))}\"><b>{esc(issue.get('title', ''))}</b></a>",
                  f"👤 <b>{esc(actor)}</b>",
                  "",
                  esc(trim(comment.get("body", ""), 500))
              ])

          elif event_name == "pull_request_review":
              review = event.get("review", {})
              pr = event.get("pull_request", {})
              state = (review.get("state") or "").upper()

              msg = lines_join([
                  f"🧾 <b>PR REVIEW {esc(state)}</b>",
                  f"📦 <b>{esc(repository)}</b>",
                  f"🔢 <a href=\"{esc(pr.get('html_url', repository_url))}\">#{pr.get('number', '')} {esc(pr.get('title', ''))}</a>",
                  f"👤 <b>{esc(actor)}</b>",
                  "",
                  esc(trim(review.get("body", ""), 500)) if review.get("body") else ""
              ])

          elif event_name == "pull_request_review_comment":
              pr = event.get("pull_request", {})
              comment = event.get("comment", {})
              path = comment.get("path", "")
              line = comment.get("line", "") or comment.get("original_line", "")

              msg = lines_join([
                  f"🗨 <b>PR REVIEW COMMENT</b>",
                  f"📦 <b>{esc(repository)}</b>",
                  f"🔢 <a href=\"{esc(comment.get('html_url', repository_url))}\">#{pr.get('number', '')} {esc(pr.get('title', ''))}</a>",
                  f"👤 <b>{esc(actor)}</b>",
                  f"📄 <code>{esc(path)}</code>" + (f":{line}" if line else ""),
                  "",
                  esc(trim(comment.get("body", ""), 500))
              ])

          elif event_name == "release":
              release = event.get("release", {})
              action = event.get("action", "")
              name = release.get("name") or release.get("tag_name", "")
              url = release.get("html_url", repository_url)
              prerelease = release.get("prerelease", False)
              body = trim(release.get("body", ""), 700)

              msg = lines_join([
                  f"🚀 <b>RELEASE {esc(action.upper())}</b>",
                  f"📦 <b>{esc(repository)}</b>",
                  f"🏷 <a href=\"{esc(url)}\"><b>{esc(name)}</b></a>",
                  f"👤 <b>{esc(actor)}</b>",
                  f"🧪 prerelease: <b>{'yes' if prerelease else 'no'}</b>",
                  "",
                  esc(body) if body else ""
              ])

          elif event_name == "create":
              ref_type = event.get("ref_type", "")
              ref = event.get("ref", "")
              master_branch = event.get("master_branch", "")

              msg = lines_join([
                  f"✨ <b>{esc(ref_type.upper())} CREATED</b>",
                  f"📦 <b>{esc(repository)}</b>",
                  f"🔤 <code>{esc(ref)}</code>",
                  f"👤 <b>{esc(actor)}</b>",
                  f"🌿 base: <code>{esc(master_branch)}</code>" if master_branch else ""
              ])

          elif event_name == "delete":
              ref_type = event.get("ref_type", "")
              ref = event.get("ref", "")

              msg = lines_join([
                  f"🗑 <b>{esc(ref_type.upper())} DELETED</b>",
                  f"📦 <b>{esc(repository)}</b>",
                  f"🔤 <code>{esc(ref)}</code>",
                  f"👤 <b>{esc(actor)}</b>"
              ])

          elif event_name == "fork":
              forkee = event.get("forkee", {})
              full_name = forkee.get("full_name", "")
              url = forkee.get("html_url", repository_url)

              msg = lines_join([
                  f"🍴 <b>REPOSITORY FORKED</b>",
                  f"📦 <b>{esc(repository)}</b>",
                  f"👤 <b>{esc(actor)}</b>",
                  f"🔗 <a href=\"{esc(url)}\">{esc(full_name)}</a>"
              ])

          elif event_name == "watch":
              action = event.get("action", "")
              msg = lines_join([
                  f"⭐ <b>WATCH {esc(action.upper())}</b>",
                  f"📦 <b>{esc(repository)}</b>",
                  f"👤 <b>{esc(actor)}</b>",
                  f"🔗 <a href=\"{esc(repository_url)}\">open repo</a>"
              ])

          elif event_name == "workflow_dispatch":
              msg = lines_join([
                  f"🛠 <b>MANUAL RUN</b>",
                  f"📦 <b>{esc(repository)}</b>",
                  f"👤 <b>{esc(actor)}</b>",
                  f"🌿 <code>{esc(ref_name)}</code>",
                  f"🔗 <a href=\"{esc(repository_url)}/actions\">actions tab</a>"
              ])

          else:
              msg = lines_join([
                  f"📣 <b>EVENT: {esc(event_name)}</b>",
                  f"📦 <b>{esc(repository)}</b>",
                  f"👤 <b>{esc(actor)}</b>",
                  f"🔗 <a href=\"{esc(repository_url)}\">open repo</a>"
              ])

          # Telegram hard limit is 4096 chars per message
          msg = msg[:4000]

          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as out:
              out.write("text<<EOF\n")
              out.write(msg + "\n")
              out.write("EOF\n")
          PY

      - name: Send to Telegram
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          THREAD_ID: ${{ secrets.TELEGRAM_MESSAGE_THREAD_ID }}
          TEXT: ${{ steps.build.outputs.text }}
        run: |
          if [ -z "$BOT_TOKEN" ] || [ -z "$CHAT_ID" ]; then
            echo "Telegram secrets are missing"
            exit 1
          fi

          if [ -n "$THREAD_ID" ]; then
            curl -sS -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
              --data-urlencode "chat_id=${CHAT_ID}" \
              --data-urlencode "message_thread_id=${THREAD_ID}" \
              --data-urlencode "text=${TEXT}" \
              --data-urlencode "parse_mode=HTML" \
              --data-urlencode "disable_web_page_preview=true"
          else
            curl -sS -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
              --data-urlencode "chat_id=${CHAT_ID}" \
              --data-urlencode "text=${TEXT}" \
              --data-urlencode "parse_mode=HTML" \
              --data-urlencode "disable_web_page_preview=true"
          fi